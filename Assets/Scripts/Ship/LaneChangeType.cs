namespace Ship {
    /// <summary>
    ///     Enumerates the intensity of lane changes the player's ship can do.
    /// </summary>
    public enum LaneChangeType {
        Normal,

        Boost,

        Turbo
    }
}